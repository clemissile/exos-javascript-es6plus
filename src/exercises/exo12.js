const exo12 = () => {
  const div = document.querySelector('#exo12');
  div.innerHTML += '<h3>Exercise 12</h3>';
  div.innerHTML += `<p>In <code>exo12.js</code>, create an <i>arrow function</i> that distructures the query parameters of an URL and adds them in an object as key-value pairs and then returns the object.</p>`;
  div.innerHTML += `<h5><u>Solution :</u></h5>`;

  // Results here
};

export default exo12;
