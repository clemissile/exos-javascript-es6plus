import companies from '../data/companies';

const exo8 = () => {
  const div = document.querySelector('#exo8');
  div.innerHTML += '<h3>Exercise 8</h3>';
  div.innerHTML += `<p>In <code>exo8.js</code>, make a new object same as <code>companies[0]</code>, using deep cloning.</p>`;
  div.innerHTML += `<h5><u>Solution :</u></h5>`;

  // Results here
};

export default exo8;
